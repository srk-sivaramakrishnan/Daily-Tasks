{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/pages/(.*)", "dest": "/server.js" },
    { "src": "/users/(.*)", "dest": "/server.js" }
  ]
}

